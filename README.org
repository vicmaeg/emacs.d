#+title: Vicmaeg's Emacs Configuration
#+author: vicmaeg

This is my personal Emacs configuration, designed to run as a daemon via
~systemd~ and used through ~emacsclient~.

* Design goals

- Daemon-first: Emacs runs in the background, frames are opened with
  ~emacsclient~.
- Modal editing with ~meow~ (selection-first, Kakoune-style): NORMAL
  state uses plain letters, and its KEYPAD gives modifier-free access
  to all Emacs keys (~SPC x f~ = ~C-x C-f~) — friendly to terminals and
  terminal multiplexers where chords like ~C->~ or ~C-.~ cannot be sent.
- Minimal GUI chrome: no menu bar, tool bar, scroll bars, or tooltips.
- Package management through ~use-package~ and MELPA.

* Starting the daemon

#+begin_src sh
  systemctl --user enable --now emacs
  systemctl --user status emacs
#+end_src

* Opening Emacs

In a terminal:

#+begin_src sh
  emacsclient -t
#+end_src

As a detached GUI frame:

#+begin_src sh
  nohup emacsclient -c -n < /dev/null > /dev/null 2>&1 &
#+end_src

* Installing or updating packages

Because the daemon has no package archives and ~use-package-always-ensure~ is
disabled in daemon mode, new packages must be installed by running Emacs
directly:

#+begin_src sh
  emacs -nw
#+end_src

After installing, restart the daemon:

#+begin_src sh
  systemctl --user restart emacs
#+end_src

* Proposed shell aliases

Add these to your =~/.bashrc= or =~/.zshrc=:

#+begin_src sh
  # Terminal frame
  alias e='emacsclient -t'

  # Detached GUI frame (survives terminal close)
  alias egui='nohup emacsclient -c -n < /dev/null > /dev/null 2>&1 &'

  # Daemon control
  alias emacsd='systemctl --user status emacs'
  alias emacsd-restart='systemctl --user restart emacs'
  alias emacsd-stop='systemctl --user stop emacs'

  # Run Emacs directly to install/update packages
  alias emacs-install='emacs -nw'
#+end_src

* Terminal (TTY) usage

The same daemon serves GUI frames (~emacsclient -c~) and terminal
frames (~emacsclient -t~).  Everything works in TTY frames; keep in
mind:

- *Nerd Font required*: icons (nerd-icons family) are rendered by the
  terminal's font.  Configure a Nerd Font in your terminal emulator or
  icons show up as boxes.
- *True color*: for themes to render exactly as in GUI, use a terminal
  with true color support (e.g. ~COLORTERM=truecolor~); otherwise
  colors fall back to the 256-color palette.
- *Corfu popup*: provided by ~corfu-terminal~ (popon overlays) in TTY
  frames; GUI frames keep native child frames.
- *diff-hl*: margin indicators are used in both GUI and TTY (the
  daemon starts headless, so frame-type checks at startup are
  meaningless).
- *Keybindings terminals cannot send* (~C-.~, ~C-;~):
  | Action      | GUI   | TTY-safe |
  |-------------+-------+----------|
  | embark-act  | ~C-.~ | ~C-c .~  |
  | embark-dwim | ~C-;~ | ~C-c ;~  |
  Selection expansion and multiple cursors need no such chords: they
  are handled by ~meow~ NORMAL-state keys and its BEACON state.

* Notable packages

** Completion framework
- ~vertico~ :: vertical completion UI
- ~marginalia~ :: rich annotations in the minibuffer
- ~orderless~ :: flexible completion matching
- ~consult~ :: search and navigation commands
- ~embark~ :: context-aware actions
- ~corfu~ :: in-buffer completion popup (uses standard capf)
- ~corfu-terminal~ :: popon-based Corfu popup for TTY frames (child
  frames in GUI frames; remove when on Emacs 31+ which has native TTY
  child frames)
- ~cape~ :: extra completion-at-point backends (dabbrev, file, keyword, elisp)
- ~nerd-icons-corfu~ :: icons in the Corfu popup

** Appearance
- ~gruber-darker-theme~ :: default theme
- ~modus-themes~, ~ef-themes~, ~doric-themes~ :: additional theme options
- ~fontaine~ :: font preset management
- ~nerd-icons~ family :: icons for dired, completion, and buffers

** Navigation and editing
- ~meow~ :: modal editing (selection-first); BEACON state for multiple
  cursors, KEYPAD for modifier-free ~C-x~/~C-c~/~M-~ access
- ~avy~ :: jump to visible text
- ~move-text~ :: move lines and regions up/down
- ~yasnippet~ :: snippets

** Project and version control
- ~project~ :: built-in project management
- ~perspective~ :: workspace-like perspectives
- ~magit~ :: Git interface
- ~diff-hl~ :: version control indicators in the fringe/margin

** Notes and organization
- ~denote~ :: simple, note-taking system
- ~denote-journal~ :: daily journal notes
- ~org-mode~ :: built-in organization and agenda

** Development
- ~lsp-mode~ :: Language Server Protocol support
- ~treesit-auto~ :: automatic tree-sitter mode remapping

** Terminal and shell
- ~ghostel~ :: terminal emulator integration
- ~eshell~ :: built-in Emacs shell

** File management
- ~dired-subtree~ :: subtree navigation in Dired
- ~trashed~ :: trash can management
